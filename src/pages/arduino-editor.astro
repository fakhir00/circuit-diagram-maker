---
import EditorPage from '../components/pages/EditorPage.astro';
import { getLocaleFromUrl } from '../theme/i18n';

const locale = getLocaleFromUrl(Astro.url.pathname);
---

<EditorPage locale={locale} editorMode="arduino" />
