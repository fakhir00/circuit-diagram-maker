---
import ComponentsPage from '../components/pages/ComponentsPage.astro';
import { getLocaleFromUrl } from '../theme/i18n';

const locale = getLocaleFromUrl(Astro.url.pathname);
---

<ComponentsPage locale={locale} />
